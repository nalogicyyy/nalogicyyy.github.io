# MySQL第一阶段‑数据库操作命令汇总表

| 功能描述 | SQL命令 | 备注 |
|---|---|---|
| 查看本机所有数据库 | `SHOW DATABASES;` | |
| 创建数据库 | `CREATE DATABASE 库名;` | 最简写法 |
| 创建数据库并指定字符集 | `CREATE DATABASE 库名 CHARACTER SET utf8mb4;` | 推荐写法，支持中文与表情 |
| 切换/使用数据库 | `USE 库名;` | |
| 查看当前所处数据库 | `SELECT DATABASE();` | |
| 修改数据库字符集 | `ALTER DATABASE 库名 CHARACTER SET utf8mb4;` | |
| 删除数据库 | `DROP DATABASE 库名;` | ⚠️不能删除当前正在使用的库 |
| 查看当前库中所有数据表 | `SHOW TABLES;` | |
| 查看数据库创建详情 | `SHOW CREATE DATABASE 库名;` | 拓展了解 |

## 重要语法规则
1. 每条SQL末尾必须带**英文分号 `;`**
2. SQL大小写不敏感，`show databases` 和 `SHOW DATABASES;`效果一样
3. 库名尽量使用英文，名称中不要带空格
4. 所有标点符号必须为**英文符号**

## 高频易错点
1. 删除数据库前，先用`use`切换到另一个数据库，否则报错
2. 单词：`databases`，不要误写为`datebases`

---

| 功能描述 | SQL命令 |
|---|---|
| 创建数据表 | `CREATE TABLE 表名(...);` |
| 删除数据表 | `DROP TABLE 表名;` |
| 查看表结构 | `DESC 表名;` |
| 插入数据 | `INSERT INTO ...;` |
| 查询数据 | `SELECT ...;` |
| 修改数据 | `UPDATE ...;` |
| 删除记录 | `DELETE ...;` |

# MySQL 增删改查笔记
## 一、4大核心关键字(CRUD)
|关键字|英文含义|作用|
|---|---|---|
|INSERT|插入|新增一行数据|
|SELECT|选择、查询|读取查看表中数据|
|UPDATE|更新|修改已经存在的数据|
|DELETE|删除|删除一行/多行数据|

## 二、SELECT 查询
### 语法
```
sql
-- 查询全部列，* 代表所有字段
SELECT * FROM student;

-- 查询指定列
SELECT name,score FROM student;

-- 带条件查询
SELECT * FROM student WHERE id=1222;
```
## 三、INSERT 插入数据
写法 1：写明字段（推荐）
```
INSERT INTO student(id,name,age,score)
VALUES (1222,'zhangshuyuan',18,99);
```
规则：字符串 / 文字必须加英文单引号，数字不需要引号
写法 2：省略字段列表
⚠️ 值顺序必须和建表字段顺序完全一致
```
INSERT INTO student
VALUES (2,'李四',19,88);
```
## 四、DELETE 删除
DELETE 删除的是整行，不能删除单独一列，不能加 *

|语句	|  作用|
|----|---|
|DELETE FROM student;	|删除表所有行，清空整张表|
|DELETE FROM student WHERE id=1222;|	只删除 id=1222 的这一行|

## 五、UPDATE 修改数据
```
UPDATE student SET name='张三' WHERE id=1222;
```
⚠️ 不加 WHERE 条件：表里所有行的数据都会被修改！
## 六、主键约束（防止 id 重复）
```
DROP TABLE student;
CREATE TABLE student(
    id int PRIMARY KEY,
    name varchar(20),
    age int,
    score double
);
```
PRIMARY KEY：主键，保证 id 唯一，不能重复。
七、易错对比表
|语句|	是否需要 *|	说明|
|----|---------|------|
|SELECT|	可以用*	|*代表所有列|
|INSERT|	不需要	|填写字段列表|
|UPDATE|	不需要|	用 WHERE 筛选行|
|DELETE|	不需要|	删除永远针对整行|
# MySQL‑单表进阶查询笔记
## 一、LIMIT 限制行数 / 分页
作用：控制查询返回多少条记录
### 语法1：只写条数
```sql
SELECT * FROM student LIMIT 条数;
```
```
-- 查询前2条学生数据
SELECT * FROM student LIMIT 2;
```

### 语法 2：偏移量，条数

> 
> ⚠️ MySQL 起始下标从 0 开始

```
SELECT * FROM student LIMIT 起始下标,条数;
```

```
-- 跳过0条，读取前2条
SELECT * FROM student LIMIT 0,2;

-- 跳过前2条，读取第3、4行
SELECT * FROM student LIMIT 2,2;
```

## 二、DISTINCT 去重

作用：去除查询结果中重复的行

```
SELECT DISTINCT 字段 FROM student;
```

```
-- 查询所有不重复的年龄
SELECT DISTINCT age FROM student;
```

## 三、AS 起别名

> 
> 别名**仅本次查询生效，不会修改数据库里真实名字**

```
SELECT name, score AS 分数 FROM student;
```

## 四、五大聚合函数（统计函数）

表格

| 函数 | 作用 |
| --- | --- |
| `COUNT()` | 统计行数 |
| `SUM()` | 求和 |
| `AVG()` | 求平均值 |
| `MAX()` | 求最大值 |
| `MIN()` | 求最小值 |

示例代码

```
--统计学生总数
SELECT COUNT(*) FROM student;

--分数总和
SELECT SUM(score) FROM student;

--平均分
SELECT AVG(score) FROM student;

--最高分
SELECT MAX(score) FROM student;

--最低分
SELECT MIN(score) FROM student;

--搭配别名
SELECT AVG(score) AS 平均分 FROM student;
```
# MySQL GROUP BY 分组 & HAVING 笔记
## 一、GROUP BY 分组查询
作用：按照某个字段，将相同值的行归为一组，一般搭配聚合函数统计。

### 语法
```sql
SELECT 分组字段, 聚合函数
FROM 表名
GROUP BY 分组字段;
```
示例：按年龄分组，统计每个年龄的学生人数
```
SELECT age,COUNT(*) AS 人数 
FROM student 
GROUP BY age;
```
规则：SELECT 后普通字段，必须出现在 GROUP BY 之后
## 二、WHERE + GROUP BY（分组前过滤）
WHERE 在分组之前筛选原始数据，不能写聚合函数
```
SELECT age,AVG(score) AS 平均分 
FROM student 
WHERE score>=60
GROUP BY age;
```
## 三、HAVING（分组之后过滤）
HAVING 在分组完成后对分组结果筛选，可以使用聚合函数
```
SELECT age,COUNT(*) AS 人数
FROM student
GROUP BY age
HAVING AVG(score)>98;
```
⚠️ 易错大坑：GROUP BY 后面不能加分号 ;，分号代表一条 SQL 结束，HAVING 就会被当成新语句报错。
## 四、WHERE 和 HAVING 对比表

|对比项|	WHERE	|HAVING|
|-----|------------|------|
|执行时机	|分组之前	|分组之后|
|是否可用聚合函数	|❌ 不可以|	✅ 可以|

## 五、完整 SQL 执行顺序
FROM → WHERE → GROUP BY → 聚合计算 → HAVING → SELECT → ORDER BY → LIMIT

