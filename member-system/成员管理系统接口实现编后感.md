    这次我完成了基于 Gin 和 GORM 的简易成员管理后端接口，实现用户注册、登录、查询成员、删除用户等基础功能。作为一名刚开始学习 Go 后端开发的新手，在编码和接口调试的过程中遇到了不少难题，也收获了很多实践经验。
    编写注册接口的时候，我只考虑了注册成功的正常流程，没有考虑前端缺少参数这类异常情况。程序出现了很费解的 bug，数据库明明没有该用户，却一直提示用户 ID 已经注册。我花费很长时间排查，才找到问题根源：请求结构体没有做好约束，前端不传 user_id，变量得到空字符串，程序拿着空字符串去数据库查询，就直接报错。这件事提醒我，写后端代码不能只关注理想运行状态，还要考虑各种参数异常的情况。
    调试接口使用 Apifox 的时候，由于我对 HTTP 相关知识掌握得不熟练，把认证 token 填到了 Params 参数里，接口一直提示没有认证令牌。后来才明白 token 需要放在 Headers 请求头里面。调试接口不光要测试成功的场景，还要测试参数错误、token 失效、权限不足等情况。
    通过这个小项目，我初步体会到分层开发的思想，控制器、模型、工具模块分工不同。同时也能看到自己的不足，很多知识点看书能够看懂，但真正动手写代码就容易出错，异常处理写得也比较简单。
    做完成员管理系统，我对 Gin 框架、GORM 数据库操作、JWT 身份登录有了更加实际的理解。很多知识光靠阅读学习体会不深，只有自己写代码、调试 bug 之后才能理解。之后写程序我会更加细心，多留意异常场景，写完之后多做测试，慢慢提升自己后端开发的能力。